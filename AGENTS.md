# PushPort Web SDK

Independent SDK repository. Product: PushPort; owner: Oleh Yurkov; contact: support@pushport.dev; Apache-2.0; initial version 0.0.1. Distribution: npm; TypeScript.

Keep platform delivery in the native SDK and preserve the documented public API. Never hardcode sibling repository paths. Use explicit local artifact/package overrides for pre-publication tests. Do not introduce Docker deployment infrastructure into an SDK repository.

No commits, pushes, tags, external publication or real notification sends without the owner's explicit instruction. Keep credentials, APNs keys, installation secrets, signing material and tokens outside source and artifacts. Check the README/testing instructions; do not report Linux/Windows checks as native iOS verification.
